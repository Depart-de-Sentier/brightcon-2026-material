# Preserve the original bytes of source data across platforms.
data/raw/** -text

# Preserve the verified export bytes and the manifest's checksums.
data/processed/ecospold2-biosphere310/** -text
*.gz binary

# PDF reports are binary documents.
*.pdf binary
